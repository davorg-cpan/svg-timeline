#!/usr/bin/perl

use strict;
use warnings;
use 5.010;

use List::Util qw[min max];
use Timeline::SVG;

my @events;
while (<>) {
  my %rec;
  @rec{qw[text start end]} = split /\t/;
  push @events, \%rec;
}

my $timeline = Timeline::SVG->new(events => \@events);

say $timeline->draw;
